

import 'package:network/network.dart';

final dioClient = RestClient(
    baseUrl: "https://demo.limerickbd.com/backend/public/api", token: '');

// getDioClient(String token) {
//   log("token:;;", error: token);
//   return RestClient(
//       baseUrl: "https://demo.limerickbd.com/backend/public/api", token: token);
// }
